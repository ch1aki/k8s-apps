repositories:
- name: argo
  url: https://argoproj.github.io/argo-helm

environments:
  development:
  production:

releases:
  - name: argocd
    namespace: argocd
    chart: argo/argo-cd
    version: 2.3.2
    values:
    - values.yaml
